# ── Core ML / training ───────────────────────────────────────────────────────
torch>=2.2.0
transformers>=4.45.0
accelerate>=0.30.0
datasets>=2.19.0
tokenizers>=0.19.0
bitsandbytes>=0.43.0
sentencepiece>=0.2.0
protobuf>=3.20.0

# ── Document extraction ───────────────────────────────────────────────────────
pymupdf>=1.24.0            # fitz – primary PDF extraction
pymupdf-fonts>=1.0.0       # extra fonts for fitz
pdf2image>=1.17.0          # converts PDF pages to PIL Images for OCR fallback
pytesseract>=0.3.10        # Tesseract OCR wrapper
Pillow>=10.0.0             # image processing
opencv-python>=4.9.0       # image pre-processing for OCR
ebooklib>=0.18             # EPUB parsing
beautifulsoup4>=4.12.0     # HTML/XML parsing (used with ebooklib)
lxml>=5.0.0                # fast XML/HTML parser backend for bs4
python-docx>=1.1.0         # DOCX parsing
chardet>=5.2.0             # encoding detection for TXT files

# ── Data processing / chunking ────────────────────────────────────────────────
numpy>=1.26.0
pandas>=2.2.0
nltk>=3.8.1                # sentence tokenisation
regex>=2024.4.0            # extended regex for text cleaning
ftfy>=6.2.0                # fix unicode text issues (OCR artefacts)

# ── Deduplication / analysis ──────────────────────────────────────────────────
datasketch>=1.6.4          # MinHash LSH near-duplicate detection
scipy>=1.13.0
scikit-learn>=1.4.0

# ── Plotting / reporting ──────────────────────────────────────────────────────
matplotlib>=3.8.0
seaborn>=0.13.0

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.66.0
pathlib2>=2.3.7            # pathlib back-compat (stdlib pathlib is fine on 3.10+)
loguru>=0.7.2              # structured logging with file + console sinks
psutil>=5.9.0              # system / GPU memory reporting
py-cpuinfo>=9.0.0
