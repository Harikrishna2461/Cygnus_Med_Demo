# CHIVA Clinical Assistant — Python dependencies
# Install with: pip install -r requirements.txt

flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0
gunicorn>=21.0.0
groq>=0.15.0
qdrant-client>=1.9.0
requests>=2.31.0
numpy>=1.26.0

# RAG Stage 1b: BM25 keyword search
rank-bm25>=0.2.2

# RAG Stage 2: cross-encoder reranking (~2GB, requires torch)
# Only install locally. On server set CROSS_ENCODER_ENABLED=False in .env to skip loading.
# sentence-transformers>=3.0.0

# Knowledge base ingestion
PyPDF2>=3.0.0
python-docx>=1.1.0

# Google Sheets logging (optional)
gspread>=6.0.0

# Agentic framework (optional — only needed when AGENTIC_MODE=true in .env)
# Uses LiteLLM under the hood; Groq is supported natively via groq/ model prefix.
crewai>=1.0.0